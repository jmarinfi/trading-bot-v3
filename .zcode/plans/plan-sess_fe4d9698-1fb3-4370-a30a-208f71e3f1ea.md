# Subplan Fase 2 — Lógica pura de Portfolio (strangler incremental) + tests

> Derivado de `REFACTOR_PLAN.md`, punto 2 de «Orden de implementación». Al aprobar, la única acción inmediata es **guardar este documento como `REFACTOR_PLAN_PHASE2.md`** — no se implementa nada hasta que lo pidas.

## Enfoque aprobado: strangler incremental

Extraer la lógica pura como métodos de la `Portfolio` actual y refactorizar `on_trade`/`on_candle` para delegar en ellos. El bot arranca en todo momento; el fix de dedup llega ya al bot vivo. La pureza total (sin exchange/store/async, estado en memoria) se completa en fases 4–5 cuando el `TradingEngine` absorbe la orquestación. División de trabajo habitual: tú implementas cada tarea; yo reviso y añado los tests.

## Re-alcance respecto al plan general

El plan original situaba en fase 2 la «carga única desde la BD y estado en memoria». Con strangler eso se traslada a fase 4 (el engine posee el estado). Fase 2 = **extracción de lógica pura + fix de dedup**, comportamiento del bot intacto salvo dos diferencias documentadas al final.

## API que construye esta fase

Métodos puros (sin I/O, sin store, sin await — testeables sin BD ni mocks). Reciben la lista de posiciones como argumento; en fase 4, cuando Portfolio posea `self._positions`, el parámetro desaparece (cambio mecánico):

| Método | Firma | Cometido |
|---|---|---|
| `find_open` | `(positions, symbol, side) -> Position \| None` | posición abierta por símbolo/lado |
| `filled_amount` | `(position) -> float` | suma de `open_fills` |
| `find_by_open_order` | `(positions, order_id) -> Position \| None` | match de fill de apertura |
| `find_by_exit_order` | `(positions, order_id) -> Position \| None` | match de fill de cierre (close/sl/tp) |
| `record_open_fill` | `(position, trade) -> bool` | dedup por `trade["id"]` + append; `True` si nuevo |
| `record_close_fill` | `(position, trade) -> bool` | dedup + append + cierre por dust; `True` si este fill cerró |
| `mark_closed` | `(position) -> None` | `is_open = False` explícito |

## Tarea 1 — `find_open()` y `filled_amount()` (refactor de `on_candle`)

**Fichero:** `src/portfolio/portfolio.py`

```python
def find_open(
    self, positions: list[Position], symbol: str, side: str
) -> Position | None:
    return next(
        (p for p in positions if p.symbol == symbol and p.side == side and p.is_open),
        None,
    )

def filled_amount(self, position: Position) -> float:
    return sum(float(fill["amount"]) for fill in position.open_fills)
```

En `on_candle`: los cuatro `next(...)` de búsqueda de posición duplicada (ramas `enter_long` ~202, `enter_short` ~243, `exit_long` ~284, `exit_short` ~304) pasan a `self.find_open(opened_positions, signal.symbol, "long"/"short")`; las cinco sumas de fills (~141, ~165, ~188, ~293, ~313) pasan a `self.filled_amount(position)`.

**Tests (míos):** `find_open` acierta con abierta correcta y devuelve `None` con lado/símbolo equivocado o posición cerrada; `filled_amount` con 0, 1 y varios fills.
**Aceptación:** pytest en verde; `import main` OK; sin cambios de comportamiento.

## Tarea 2 — `find_by_open_order()` y `find_by_exit_order()` (refactor de `on_trade`)

**Fichero:** `src/portfolio/portfolio.py`

```python
def find_by_open_order(
    self, positions: list[Position], order_id: str
) -> Position | None:
    return next((p for p in positions if p.open_order["id"] == order_id), None)

def find_by_exit_order(
    self, positions: list[Position], order_id: str
) -> Position | None:
    return next(
        (
            p
            for p in positions
            if (p.close_order and p.close_order["id"] == order_id)
            or (p.sl_order and p.sl_order["id"] == order_id)
            or (p.tp_order and p.tp_order["id"] == order_id)
        ),
        None,
    )
```

En `on_trade` (~74–90): los dos `next(...)` pasan a usar estos métodos.

**Tests:** match por `open_order`; por cada tipo de orden de salida; posiciones con órdenes `None` no rompen; id desconocido → `None`.

## Tarea 3 — `record_open_fill()` con dedup por `trade["id"]`

**Fichero:** `src/portfolio/portfolio.py`

```python
def record_open_fill(self, position: Position, trade: Trade) -> bool:
    if any(fill["id"] == trade["id"] for fill in position.open_fills):
        return False
    position.open_fills.append(trade)
    return True
```

En `on_trade` (~78–79):

```python
if position is not None:
    if not self.record_open_fill(position=position, trade=trade):
        log.info("fill de apertura duplicado ignorado (trade %s)", trade["id"])
```

**Tests:** fill nuevo se añade y devuelve `True`; duplicado no se añade y devuelve `False`; ids distintos ambos se añaden.

## Tarea 4 — `record_close_fill()` con dedup + cierre por dust; `mark_closed()`

**Fichero:** `src/portfolio/portfolio.py`

```python
def mark_closed(self, position: Position) -> None:
    position.is_open = False

def record_close_fill(self, position: Position, trade: Trade) -> bool:
    if any(fill["id"] == trade["id"] for fill in position.close_fills):
        return False
    position.close_fills.append(trade)
    sum_open = self.filled_amount(position)
    sum_close = sum(float(fill["amount"]) for fill in position.close_fills)
    if abs(sum_open - sum_close) < calculate_dust_value(trade["price"]):
        self.mark_closed(position)
        return True
    return False
```

En `on_trade` (~91–101):

```python
if position is not None:
    if self.record_close_fill(position=position, trade=trade):
        await self._cancel_sl_order(position=position)
```

El prune de `on_candle` (~118–121):

```python
if not position.open_fills:
    await self._cancel_sl_order(position=position)
    self.mark_closed(position)
    continue
```

**Tests:** cierre parcial no cierra; igualar por debajo del dust cierra (`is_open=False`, `True`); duplicado no cuenta doble ni dispara cierre; frontera exacta del dust.

## Tarea 5 — Verificación de cierre de fase

- `uv run pytest` completo en verde (27 existentes + nuevos de `tests/test_portfolio.py`).
- `uv run python -c "import main"` sin excepciones.
- Arranque manual corto (arrancar y Ctrl-C).
- Marcar fase 2 completada en `REFACTOR_PLAN.md`.

## Cambios de comportamiento (los únicos)

1. **Dedup de fills activo en el bot vivo** — fix real: los fills reenviados tras reconexión (o presentes tras reinicio; los ids sobreviven al roundtrip JSON de la BD) ya no corrompen las sumas.
2. En `on_trade`, `is_open` pasa a `False` antes del `cancel_sl` (antes: después). Sin impacto funcional; el cancel es best-effort.

## Fuera de alcance

- Estado en memoria / carga única desde BD → fase 4 (engine).
- Carreras on_trade/on_candle → fase 4–5 (cola de un solo consumidor).
- Bug `_there_is_coin` sin `await` (~210, ~251) → fase 3 (`OrderService.has_balance`).
- `return` prematuro de `on_candle` (~194) que pierde mutaciones → fase 4 (save tras cada mutación).

## Orden y dependencias

```
T1 ─► T2 ─► T3 ─► T4 ─► T5   (secuencial: cada una refactoriza código que la siguiente toca)
```

T1 y T2 son extracción mecánica; T3 y T4 introducen el dedup (única lógica nueva); T5 cierra.

---

**Acción al aprobar:** escribir este documento en `REFACTOR_PLAN_PHASE2.md` y detenerse ahí.